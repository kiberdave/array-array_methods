## 10. ელ-ფოსტების ფილტრაცია (`filter` + `includes`)

**პირობა:** მოცემულია ელ-ფოსტების სია. გამოიყენე `filter` მეთოდი და ამოარჩიე მხოლოდ ის ელ-ფოსტები, რომლებიც მთავრდება `"@gmail.com"`-ით.

```javascript
const emails = ["user@gmail.com", "admin@yahoo.com", "test@gmail.com", "hello@outlook.com"];

// შენი კოდი აქ...

// მოსალოდნელი შედეგი: ["user@gmail.com", "test@gmail.com"]
```

---

## 11. სახელების გაერთიანება (`join`)

**პირობა:** მოცემულია გუნდის წევრების სახელები. `join` მეთოდის გამოყენებით შექმენი ერთი სტრიქონი, სადაც სახელები გამოყოფილია `" | "`-ით.

```javascript
const team = ["Ana", "Giorgi", "Nino", "Davit"];

// შენი კოდი აქ...

// მოსალოდნელი შედეგი: "Ana | Giorgi | Nino | Davit"
```

---

## 12. პროდუქტების დასახელება დიდი ასოებით (`map`)

**პირობა:** მოცემულია პროდუქტების სახელები პატარა ასოებით. `map` და `toUpperCase()` მეთოდების გამოყენებით შექმენი ახალი მასივი, სადაც ყველა სახელი დიდი ასოებით იქნება.

```javascript
const products = ["laptop", "phone", "tablet", "monitor"];

// შენი კოდი აქ...

// მოსალოდნელი შედეგი: ["LAPTOP", "PHONE", "TABLET", "MONITOR"]
```

---

## 13. ყველაზე მაღალი ქულა (`reduce`)

**პირობა:** მოცემულია სტუდენტების ქულები. `reduce` მეთოდის გამოყენებით იპოვე ყველაზე მაღალი ქულა.

```javascript
const scores = [67, 92, 45, 88, 73, 99, 56];

// შენი კოდი აქ...

// მოსალოდნელი შედეგი: 99
```

---

## 14. კალათის ჯამი (`reduce`)

**პირობა:** მოცემულია საყიდლების კალათის პროდუქტები ფასებით. `reduce` მეთოდის გამოყენებით გამოთვალე სრული ჯამი.

```javascript
const cart = [
  { name: "Bread", price: 3.5 },
  { name: "Milk", price: 2.8 },
  { name: "Cheese", price: 7.2 },
  { name: "Eggs", price: 4.0 }
];

// შენი კოდი აქ...

// მოსალოდნელი შედეგი: 17.5
```

---

## 15. დუბლიკატების წაშლა (`filter` + `indexOf`)

**პირობა:** მოცემულია მასივი განმეორებადი რიცხვებით. `filter` და `indexOf` მეთოდების გამოყენებით შექმენი ახალი მასივი მხოლოდ უნიკალური რიცხვებით.

```javascript
const numbers = [1, 2, 3, 2, 4, 3, 5, 1];

// შენი კოდი აქ...

// მოსალოდნელი შედეგი: [1, 2, 3, 4, 5]
```

---

## 16. მასივის შებრუნება (`reverse`)

**პირობა:** მოცემულია სიტყვების მასივი. `reverse` მეთოდის გამოყენებით შებრუნე მასივი. ყურადღება: `reverse` ცვლის ორიგინალ მასივს — ამიტომ ჯერ `slice()`-ით გადაიღე ასლი.

```javascript
const words = ["Hello", "World", "JavaScript", "Arrays"];

// შენი კოდი აქ...

// მოსალოდნელი შედეგი: ["Arrays", "JavaScript", "World", "Hello"]
```

---

## 17. ონლაინ მომხმარებლები (`filter` + `find`)

**პირობა:** მოცემულია მომხმარებლების სია სტატუსებით. ჯერ `filter`-ით ამოარჩიე მხოლოდ ონლაინ მომხმარებლები, შემდეგ `find`-ით იპოვე პირველი ადმინი ონლაინ მომხმარებლებს შორის.

```javascript
const users = [
  { name: "Nika", online: false, role: "admin" },
  { name: "Salome", online: true, role: "user" },
  { name: "Beka", online: true, role: "admin" },
  { name: "Tamar", online: true, role: "user" }
];

// შენი კოდი აქ...

// მოსალოდნელი შედეგი (filter): [{ name: "Salome", ... }, { name: "Beka", ... }, { name: "Tamar", ... }]
// მოსალოდნელი შედეგი (find):   { name: "Beka", online: true, role: "admin" }
```

---

## 18. ტემპერატურის კონვერტაცია (`map`)

**პირობა:** მოცემულია ტემპერატურები ცელსიუსში. `map` მეთოდის გამოყენებით გადაიყვანე ყველა მნიშვნელობა ფარენჰეიტში. ფორმულა: `F = C * 9/5 + 32`.

```javascript
const celsius = [0, 20, 37, 100];

// შენი კოდი აქ...

// მოსალოდნელი შედეგი: [32, 68, 98.6, 212]
```
